@AGENTS.md

## Deployment

- Server: SSH alias `to-do` (e.g. `ssh to-do`)
- App directory on server: `REDACTED-PATH`
- Process manager: pm2 (process name: `watchns`)
- Deploy: push to origin, SSH in, run `bash deploy.sh`
